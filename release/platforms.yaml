# Default release target. Restore additional targets using deploy/README.md.
platforms:
  - os: linux
    arch: amd64
    runner: ubuntu-latest
    target: x86_64-unknown-linux-gnu
    binary: codex-proxy-rs
    archive: tar.gz
    docker: true
    dockerPlatform: linux/amd64
