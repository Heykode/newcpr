const now = new Date().toISOString()
export const accounts = [0, 1, 2].map((reloginCount, index) => ({
  id: `acct_sample_${index}`,
  name: `sample-${index}`,
  provider: 'openai',
  resourceRef: `acct_sample_${index}`,
  email: `sample-${index}@example.invalid`,
  accountId: `workspace-${index}`,
  userId: `user-${index}`,
  label: null,
  planType: index ? 'team' : 'free',
  planTypeDisplay: index ? 'Team' : 'Free',
  authenticationKind: 'oauth',
  hasRefreshToken: true,
  reloginCount,
  lastReloginAt: reloginCount ? now : null,
  status: 'normal',
  errorReason: null,
  errorMessage: null,
  enabled: true,
  inFlight: 0,
  concurrencyLimit: null,
  effectiveConcurrencyLimit: 3,
  weight: 100,
  outboundProxyEndpoint: null,
  accessTokenExpiresAt: '2099-01-01T00:00:00Z',
  accessTokenExpiresAtDisplay: '2099-01-01',
  refreshTokenExpiresAt: null,
  nextRefreshAt: null,
  addedAt: now,
  addedAtDisplay: '2026-09-16',
  updatedAt: now,
  updatedAtDisplay: '2026-09-16',
  groups: [],
  healthTimeline: [],
  cumulativeCosts: [],
  quota: { windows: [], limitReached: false, rateLimitedUntil: null, refreshedAtDisplay: '—' },
  usage: {
    requestCount: 0,
    successCount: 0,
    inputTokens: 0,
    outputTokens: 0,
    totalTokens: 0,
    totalTokensDisplay: '0',
    costs: [],
    models: [],
    quotaWindow: null,
    lastUsedAt: now,
    lastUsedAtDisplay: '刚刚',
  },
}))

export const reloginEntries = accounts.map(account => ({
  id: `relogin_${account.id}`,
  revision: 1,
  email: account.email,
  hasTotp: account.id !== 'acct_sample_2',
  automatic: true,
  status: 'ready',
  message: '凭据已同步到号池',
  planType: account.planType,
  workspaceId: account.accountId,
  preferredWorkspaceId: null,
  credentialStatus: 'verified',
  poolStatus: 'synced',
  poolAccountIds: [account.id],
  reloginAccountId: account.id,
  reloginCount: account.reloginCount,
  lastReloginAt: account.lastReloginAt,
  verifiedAt: now,
  expiresAt: account.accessTokenExpiresAt,
  updatedAt: now,
}))
